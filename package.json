{
  "name": "node-app",
  "private": true,
  "type": "module",
  "workspaces": ["server", "web", "shared", "e2b"],
  "scripts": {
    "dev:server": "bun run --cwd server dev",
    "dev:web": "bun run --cwd web dev",
    "typecheck": "bun run --filter '*' typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "bun-types": "^1.3.0"
  }
}
